<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ParkingLotMapper">

    <select id="selectAll" resultType="com.example.entity.ParkingLot">
        select parking_lot.*, location.name as locationName,
               DATE_FORMAT(parking_lot.deleted_at, '%Y-%m-%d %H:%i:%s') as deletedAt 
        from parking_lot
        left join location on parking_lot.location_id = location.id
        <where>
            <if test="id != null"> and parking_lot.id = #{id}</if>
            <if test="name != null"> and parking_lot.name like concat('%', #{name}, '%')</if>
            <if test="locationId != null"> and parking_lot.location_id = #{locationId}</if>
            <if test="locationName != null"> and location.name like concat('%', #{locationName}, '%')</if>
            <if test="status != null"> and parking_lot.status like concat('%', #{status}, '%')</if>
            and parking_lot.deleted_at is null
        </where>
    </select>

    <select id="selectById" resultType="com.example.entity.ParkingLot">
        select parking_lot.*,
               DATE_FORMAT(parking_lot.deleted_at, '%Y-%m-%d %H:%i:%s') as deletedAt 
        from `parking_lot`
        where id = #{id} and deleted_at is null
    </select>

    <delete id="deleteById">
        delete from `parking_lot`
        where id = #{id}
    </delete>

    <update id="softDeleteById">
        update `parking_lot` 
        set deleted_at = NOW()
        where id = #{id}
    </update>

    <insert id="insert" parameterType="com.example.entity.ParkingLot" useGeneratedKeys="true" keyProperty="id">
        insert into `parking_lot`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="locationId != null">location_id,</if>
            <if test="status != null">status,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="locationId != null">#{locationId},</if>
            <if test="status != null">#{status},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.ParkingLot">
        update `parking_lot`
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="locationId != null">
                location_id = #{locationId},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id} 
    </update>

</mapper>